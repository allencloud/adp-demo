apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "adp-demo.fullname" . }}-test-connection"
  labels:
    {{- include "adp-demo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "adp-demo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
